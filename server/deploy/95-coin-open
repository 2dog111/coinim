#!/bin/sh
count=$(find /var/lib/coin-im-open/notifications -maxdepth 1 -type f -name 'job_*.json' 2>/dev/null | wc -l)
if [ "$count" -gt 0 ]; then
  printf 'coin.im Business Files waiting: %s (run sudo coin-open notifications)\n' "$count"
fi
