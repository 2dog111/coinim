# coin.im Versions

This is the local place to check the current site version before edits or deployment.

## Current Version

- Local site version: `2026-08-29-three-slot-checkout-mechanics`
- Active VPS release: `/var/www/coin.im/releases/20260829T155312Z`
- Active takeover backend release: `/opt/coin-im-market/releases/20260829T155312Z`
- 2026-08-29 Git checkpoint redeploy: the unchanged 118-file production package and backend version `2026.08.29.8` were released together as `20260829T155312Z` before committing the complete working tree. The database backup is `/var/lib/coin-im-market/backups/market-20260829T155315Z.sqlite3`; integrity remains `ok` with 3 messages, 3 reigns, 2 existing payment intents and 0 payments. Local and remote package manifests match at SHA-256 `06e0213feebdab8e1f31e16e3c9fb2961e1a787d707e86daf69a1973ac56a90c`. Production readiness and smoke checks pass, all four checked services are active, and the root, all three slot forms, Archive, Stats, Rules, market health, mail, Market Scan, handling, open intake and their required assets return HTTP 200.
- 2026-08-29 checkout-mechanics release: the public root now explains the complete three-slot purchase path in place. Slot № 1 accepts a website headline, 111 to 888 non-space-character description and public non-social URL; slot № 2 accepts a 111 to 888-character pitch, optional note and supported public social-profile URL; slot № 3 accepts a 111 to 888-character text-only message and optional name, with no public URL. Each type-specific Outbid button opens only its matching form, identifies the current placement and price, and states the exact price at current plus `1 USDT`. Form submission creates a private seven-minute receipt without charging or publishing. The receipt identifies the selected slot, shows the exact USDT TRC-20 amount and receiving address, QR code, TronLink action, automatic monitoring and manual transaction-ID fallback. A final matching transfer replaces only that slot, moves its predecessor to Archive and leaves the other two slots unchanged. Plain `/takeover` redirects to `/#how-it-works`; the retired generic whole-page form is no longer a public entry path. Backend version `2026.08.29.8`, wall cache key `20260829-wall5`, and market cache key `20260829-market10` are active. The release backup is `/var/lib/coin-im-market/backups/market-20260829T153152Z.sqlite3`. Database integrity is `ok`; 3 messages, 3 reigns, 2 existing payment intents and 0 payments are preserved. The clean 118-file local and remote manifests match at SHA-256 `06e0213feebdab8e1f31e16e3c9fb2961e1a787d707e86daf69a1973ac56a90c`. Thirty public HTTPS targets passed, readiness and production smoke passed, Nginx and all four checked services are active, and the coin.im certificate remains valid through 2026-11-13. Local Chromium `151.0.7922.34` passed the full width matrix and a complete mock website, Telegram-profile and message payment journey; public Chromium passed the root and all three read-only forms at `390x844` and `1440x900` with no overflow or console, page, request or HTTP errors. Public QA created no payment intent or transaction.
- 2026-08-29 three-slot outbid release: the complete attached 146-line redesign specification and the later logo correction are active on `https://coin.im/`. The root has exactly three database-backed occupied placements at `14`, `12`, and `10 USDT`; every Outbid action computes the selected slot price plus `1 USDT`, opens the existing checkout with the slot and amount prefilled, and a confirmed payment replaces only that slot while archiving its prior placement. Slot four and its claim path are absent from the root source and rendered page. The new `76px` app icon depicts three paid message rows and three coins with restrained depth instead of the rejected rainbow letter concept; all `512`, `192`, `180`, `32`, and `16` PNG sizes are deployed. Backend version `2026.08.29.7`, migration `005_three_wall_slots`, wall cache key `20260829-wall4`, and market cache key `20260829-market9` are active. Final backup `/var/lib/coin-im-market/backups/market-20260829T151329Z.sqlite3` has integrity `ok` and preserves the migrated three-slot state before the final synchronization release; the pre-migration backup remains `/var/lib/coin-im-market/backups/market-20260829T150801Z.sqlite3`. The live database has integrity `ok`; only the expected migration and three initial wall messages and reigns were added, while the existing two payment intents and every payment, result, credit, lock, notification, and outbox count stayed unchanged. Twenty-eight public HTTPS targets returned 200, local and remote release hashes match, all five route mirrors match, Nginx and all four checked units are active, readiness, reconciliation, and production smoke pass, and the certificate is valid through 2026-11-13. Chromium `151.0.7922.34` passed the local root at `375`, `390`, `393`, `430`, `768`, `1024`, `1440`, and `1920`, a complete temporary mock outbid and Archive journey, then the public root at `390x844` and `1440x900` plus a read-only click through to the prefilled slot-one checkout. Public QA created no payment intent or transaction.
- 2026-08-29 fixed-wall release: the attached 490-line master prompt was applied to the exact public root and its byte-identical static fallback. The homepage now has three fixed occupied placements: `pen.dev` at `14 USDT`, `@midnightdrafter` at `12 USDT`, and `On silence.` at `10 USDT`; the fourth slot is open at `11 USDT`. The real `pen.dev` screenshot is `229459` bytes. The whole website and social cards are links, the reading card is not, and the claim action intentionally remains `href="#"` because payment connection is a later task in the prompt. Backend version `2026.08.29.6` and wall cache key `20260829-wall3` are active. Database backup `/var/lib/coin-im-market/backups/market-20260829T093525Z.sqlite3` passed integrity checks and has the same per-table counts as the live database after release. Twenty-three public HTTPS targets returned 200; root HTML, static files and backend files match local SHA-256 hashes; Nginx and all three application units are active; environment, reconciliation and public production smoke checks passed. Chromium `151.0.7922.34` passed local widths `375`, `390`, `393`, `430`, `768`, `1024`, `1440`, and `1920`, then public `390x844` and `1440x900`, with four slots, loaded imagery, font weights no greater than 500, first-slot visibility, no overflow, and no console, page, network, or HTTP errors. No payment intent or transaction was created.
- 2026-08-29 static redeploy: the clean 105-file allowlist was published as `/var/www/coin.im/releases/20260829T071346Z`. It is byte-identical to the preceding active static release and keeps the existing cache keys. Twenty-six public HTTPS checks returned 200; all five route mirror pairs match; Nginx configuration is valid; `nginx`, `coin-im-market`, `coin-im-market-reconcile.timer`, and `coin-im-open` are active; the coin.im certificate is valid through 2026-11-13. Local and public isolated Chromium `151.0.7922.34` passed `/ms` at `390x844` and `1440x900`; public Chromium also passed the root takeover dialog, real click, message fill, live preview, keyboard focus, console, page, network, HTTP, and horizontal-overflow checks at both viewports. No payment intent, transaction, or transfer was created. Read-only reconciliation reports zero active reigns and zero orphan payments. The takeover backend, encrypted intake data, DNS, and mail routing were not changed.
- English Market Scan reader release: `/ms` and `/ms/` are byte-identical at SHA-256 `29cb69ad863e4ef747adc6c2cad125b0c65f560ec66605d88adb341ee9bd6e59`. The page is English-only, uses a pale yellow reading surface, black Source Serif 4 text, a 65-character measure, an unnumbered single-column longread, a responsive evidence table, and the seven-colour liquid-glass mail icon. The previous chapter UI, badges, counters, FAQ, Russian switch, and `/msru` references remain absent from this route.
- Market Scan Anton copy pass: the complete visible story now stays with the author in first person, beginning with the words that were missing from paid database searches and continuing through the four builds, route verification and 15-run benchmark. The visible body moved from 2,906 to 2,995 words as generic explanations were rebuilt around the author's actions and costs. Every numeric token count from the preceding public page and all named products, models, prices, dates, benchmark cells and limitations remain present.
- Market Scan reader assets: `assets/ms.css?v=20260829-reader2` hashes to `5692926e66ed789ff63eac1beb89c2bab6e72d3fcd484b6f640198dcbe8bebe9`; the Postmark reference informed only the compact paper evidence cards, blue and yellow offset shadows, and physical button press states. `assets/coin-im-market-scan-glass-20260829.webp` remains `c69d95a284f5c5993048fcaea1780cb34e0c75241cd1a6f49e8decf139a4f188`; `assets/ms-contact.js?v=20260829-reader` is unchanged.
- Market Scan reader QA: local isolated Chromium `151.0.7922.34` passed `375x812`, `390x844`, `393x852`, `430x932`, `768x1024`, `1024x768`, `1440x900`, and `1920x1080`; public Chromium passed `390x844` and `1440x900`. The complete longread markers, first evidence and benchmark comparison cards, keyboard skip link, main CTA navigation to `/open`, console, page, network, HTTP, and horizontal-overflow checks passed. The prior Mobile Safari on iOS Simulator `26.5` coverage remains the latest iOS-specific check; this copy and CSS micro-polish did not repeat the iOS matrix.
- Git base: `e6cf42f` on `main`; the `/msru` copy, sales, and reference UI passes plus the English reference translation are uncommitted
- Funnel: `/` is a database-backed wall of exactly three occupied placements. Each card links to `/takeover` with its slot and current price plus `1 USDT`; confirmation replaces only the target slot and sends the previous placement to `/archive`. `/mail`, `/ms`, and `/open` remain separate.
- New Russian copy on `/ru` only: heading `Market Scan целиком`, one sentence under it, button `Читать Market Scan` (twice). Everything that was on the page before is untouched
- Russian home stylesheet cache key: `assets/ru.css?v=20260818-e3`; the other four stylesheets stay at `?v=20260818-e2`
- Shared base CSS cache key in HTML: `assets/base.css?v=20260818-e2` (/, /msru, /open; `/ms` now uses its standalone reader stylesheet)
- Home CSS cache key in HTML: `assets/home.css?v=20260818-e2` (also loaded by /open)
- Russian home page stylesheet: `assets/ru.css?v=20260818-e2` (ru.html no longer loads `styles.css`; `styles.css` still serves /es and stays deployed)
- Business File CSS cache key in HTML: `assets/open.css?v=20260818-e2`
- Business File JS cache key in HTML: `assets/open.js?v=20260815-open3` (JS unchanged)
- English Market Scan CSS cache key in HTML: `assets/ms.css?v=20260829-reader2`
- Russian Market Scan reference UI cache keys: `/msru` loads `assets/msru.css?v=20260828-editorial2` and `assets/msru.js?v=20260828-editorial2`; English `/ms` no longer loads them
- Takeover stylesheet and JavaScript cache key: `20260829-market10`; paid-wall stylesheet and JavaScript key: `20260829-wall5`.
- Three-slot release hashes: `index.html` and backend `home.html` are `9c1d345ea5a5f12beb16ebb59dacdead1998a7ada4faafc043e12eb73afe862a`; `assets/wall.css` is `2a36d5dece931e696b7d45ed4655f2c24523863c0df7a7deb6e32290d3a8b239`; `assets/wall.js` is `1cfe5dca1622b5e3e3048ea11c055f11974c73195544664a820a80cbd0c633f6`; `assets/takeover.css` is `a88440a7c5e5025ae6de499c277514bc0dedbce0063ff799900f586b894de711`; `assets/takeover.js` is `37dff389bc72ec8acecb24a492bc646e6df60e627ad11997a9e2a4e9293f7cd2`. Backend `render.py` is `b870b93a18232bdcb1706e85f827b9c4664d0978fd6569364254baa051427427`; `market.py` is `27828a21499bf60fad6e56ff01acdf194d96698caaedf6ec0bccd1fb0208a52c`; `app.py` is `5e7a5aadac82714c49a084c9f68f8f49d706007dab1ed0f24da4b2e55d577b4c`; `__init__.py` is `9bdfd024b0b083439708342115a7d060820e10e097e84b34d5a050ea9dbee667`.
- Legacy audit found no fixed-slot checkout, future paid slots, or confirmed placement payments in the deployed source or data stores. The new market schema therefore starts empty and leaves the encrypted `/open` data untouched.
- Local market QA: 32 unit and SQLite integration tests passed, including the three entity-specific form contracts, exact per-slot pricing, receipt details, automatic transaction discovery, QR data, TronLink callback, archive, payment invariants and target-only replacement. Chromium `151.0.7922.34` passed root widths `375`, `390`, `393`, `430`, `768`, `1024`, `1440`, and `1920` with three slots, dynamic `36 USDT` total, no overflow, no failed imagery, minimum `16px` type, and maximum weight `500`. A fresh temporary mock database completed all three payments at `15`, `13`, and `11 USDT`, rendered Telegram correctly, archived all three predecessors and produced the next prices `16`, `14`, and `12 USDT`.
- Production market QA: backend `2026.08.29.8` is active in both release paths `20260829T153149Z`; migrations 001 through 005 are applied. The production readiness gate reports every check true; health reports provider `trongrid`, `marketEnabled=true`, database connectivity and the current version. Reconciliation reports zero orphan payments. Nginx, `coin-im-market`, `coin-im-market-reconcile.timer`, and `coin-im-open` are active. Public Chromium passed the root and every slot-specific form at `390x844` and `1440x900`, with no overflow and no console, page, request or HTTP errors. The read-only public checks preserved both existing payment intents and created no transaction or transfer.
- Homepage and `/mail` route check: the static fallback `index.html` and backend `home.html` are byte-identical at `9c1d345ea5a5f12beb16ebb59dacdead1998a7ada4faafc043e12eb73afe862a`. Production `mail`, `ms`, `msru`, `handling`, and `open` file pairs remain byte-identical. The clean 118-file static allowlist preserved every existing route.
- Public-messenger QA: isolated Chromium `151.0.7922.34`, Firefox `153.0`, and Playwright WebKit `26.5` passed the complete composer journey without console, page, request, HTTP, or horizontal-overflow errors. Mobile Safari on iOS Simulator `26.5` passed real tap, type, 500-character, preview, close, scroll, and DOM checks on QA iPhone SE (3rd generation), QA iPhone 17, and QA iPhone 17 Pro Max. The final public no-link preview correction passed Chromium, WebKit, and Mobile Safari on QA iPhone 17 Pro Max. Local Lighthouse scored Performance 97, Accessibility 100, Best Practices 100, and SEO 92; the SEO deduction is the intentional nonstandard `Content-Signal` directive in `robots.txt`.
- Russian Market Scan copy pass: `6750` to `6018` words by removing repeated summaries and explanations; unique facts, benchmark data, model names, three `/open` CTAs, and the five-part structure are preserved
- Russian Market Scan sales pass: the hero now leads with the expensive decision before buying a list; metadata states the concrete result; the current manual intake is separated from benchmark autonomy; qualification and the next step are explicit; all four `/open` actions use `Передать материалы для скана`
- English Market Scan reference pass: `/ms` is a complete English translation of the current Russian source, with the same 165 protected H1, H2, H3, and paragraph nodes, the same five-part order, benchmark, manual-intake boundary, image, navigation, and four `/open` actions
- Russian conversion-story pass: the hero now uses the documented 17-year list history as the bridge into the story; the early intake button, `Читать историю`, part marker, resume prompt, and mobile contents block are removed; the five section labels and all 25 H3 headings are concrete outcome-led reading hooks; `/msru` now has 163 protected H1, H2, H3, and paragraph nodes and three established `/open` actions
- Russian first-person opening pass: the first screen states the list blind spot in plain language, explains it from the author's own experience, and ends on the physical letter; part one continues in first person with the old list workflow and its cost. The delayed rail, benchmark, three in-story `/open` actions, and analytics remain unchanged.
- Site currency-copy pass: every exact monetary amount in the public HTML uses digits and a currency symbol; Russian and English Market Scan no longer use the empty `Не потому... Потому что...` or `Not because... Because...` scaffold, and the remaining sentence-leading reason fragments were joined to their claims. Permanent rules are in `AGENTS.md`.
- Russian wide-opening pass: `/msru` now opens with a new vocabulary-limit proposition, explains the filter mechanism and documented list spend beside it, removes the key photograph from the hero, and uses a wide two-column handoff into the first-person story on desktop. Below 900 px the same material is one linear column. The pass is deployed and publicly verified in `/var/www/coin.im/releases/20260827T183810Z`.
- Local Russian editorial-dossier pass: `/msru` keeps every visible word from the current public page and replaces only presentation. The image-free hero, all five chapters, proof inserts, benchmark, qualification note, and final CTA now use one print-dossier system with warm paper, ink, wax terracotta, chapter counters, drop caps, a dark evidence table, and a sealed-envelope close. The desktop grid never alternates sides. The sticky header hides down and returns up; the delayed rail still waits two minutes and part two. This pass is local only; production remains `/var/www/coin.im/releases/20260827T183810Z`.
- Russian editorial-dossier v2 pass: the 133 protected author paragraphs remain byte-identical and in the original order. Five chapter headings and the factually wrong benchmark subheading were replaced with concrete, table-consistent interface headings and logged in `changelog-copy.md`. The page adds a hero passport, dynamic reading estimate, sticky five-chapter strip, five proof notes, one exact quote spread, benchmark context and the calculated 4.4× density gap, two quiet actions, an anti-filter block, result inventory, four-item FAQ, Article plus FAQPage JSON-LD, footer, and a 1200 by 630 Russian OG image. The final CTA is still the only loud action. English `/ms` is unchanged. This pass is deployed in `/var/www/coin.im/releases/20260828T052802Z`.
- English editorial-dossier v2 port: `/ms` now carries the same image-free hero, stable chapter grid, delayed rail, chapter strip, proof notes, quote spread, benchmark evidence, qualification block, result inventory, FAQ, four `/open` actions, metadata layers, and restrained closing composition as `/msru`. All current Russian source facts and story beats are present in English in the same order. The five established English hashes remain compatible. Automatic English paragraph hyphenation is disabled to prevent browser-generated word breaks. The English OG image is 1200 by 630 pixels and 336,822 bytes.
- `ms.html` and `ms/index.html` SHA-256: `6e997674e08f98fd35e36abafb26cd6058b43c490fd3fe23de99f8b0995a4142`
- `msru.html` and `msru/index.html` SHA-256: `a87a5a1c547d5634d9fe604a52415351728ce99917e4b760e93a577fec4d39d2`
- The shared reference UI provides the physical-letter hero, responsive contents, section-navigation script, benchmark presentation, and closing action. The only English-specific visual rule widens and scales the longer hero heading and disables automatic English body hyphenation
- Body/heading font: `assets/fonts/source-serif-4-variable.woff2` (Source Serif 4 Variable, latin + cyrillic subset, 198 KB, preloaded on /, /ru, /ms, /msru, /open)
- 2026-08-27 English reference deploy verified: 21 public URLs return 200 over HTTPS, `/api/open/health` and `www` are OK, public and direct-origin `/ms`, `/ms/`, `/msru`, and `/msru/` match local SHA-256 values, and production Chromium checks pass at `390x844` and `1440x900`
- Note: the London microcement passage in `/ms` and `/msru` sat restored in the working tree in both languages (uncommitted parallel-session state after commit `8b0571e` cut it); it is preserved and deployed
- 2026-08-28 local Russian opening check: Chromium `151.0.7922.34`, Firefox `153.0`, and Playwright WebKit `26.5` covered `390x844` and `1440x900`; Chromium also covered `1920x1080` and the delayed rail simulation. The first screen, first story viewport, unpinned header, keyboard focus, delayed rail, non-overlap, and horizontal overflow passed without console, page, network, or HTTP errors. Firefox used viewport screenshots because its full-page screenshot limit is below the document height. No public verification was run because this pass was not deployed.
- 2026-08-28 currency-copy deploy verified: 21 public HTTPS URLs return 200; public and direct-origin `/ms`, `/ms/`, `/msru`, and `/msru/` match local SHA-256 values; public `/ru`, `/s.html`, and `/ai.html` also match local files. Chromium `151.0.7922.34` passed `/ms`, `/msru`, `/ru`, `/s.html`, and `/ai.html` at `390x844` and `1440x900` with the new markers visible, no horizontal overflow, and no console, page, network, or HTTP errors. Active release: `/var/www/coin.im/releases/20260827T180854Z`.
- 2026-08-28 local Russian wide-opening check: Chromium `151.0.7922.34` passed `/msru.html` at `390x844`, `1440x900`, `1920x1080`, and `2048x1152`; Firefox `153.0` and Playwright WebKit `26.5` passed viewport checks at `390x844` and `1440x900`. The image-free hero, new copy, wide story handoff, keyboard focus, delayed desktop rail, non-overlap, and horizontal overflow passed with no console, page, network, or HTTP errors. Firefox used viewport screenshots because its full-page screenshot limit is below the document height. No public check or deployment was run for this pass.
- 2026-08-28 Russian wide-opening deploy verified: the clean `89`-file release is `/var/www/coin.im/releases/20260827T183810Z`; all 21 required public HTTPS URLs return 200; public and direct-origin `/ms`, `/ms/`, `/msru`, and `/msru/` match the local SHA-256 values above. Public Chromium `151.0.7922.34` passed `/msru` at `390x844`, `1440x900`, and `2048x1152`: the new opening, image removal, story handoff, keyboard focus, CTA to `/open`, delayed desktop rail, non-overlap, and horizontal overflow passed without console, page, network, or HTTP errors. Nginx and `coin-im-open` are active; the backend remains `/opt/coin-im-open/releases/20260817T170404Z`.
- 2026-08-28 local Russian editorial-dossier check: the normalized visible copy is byte-equivalent to current public `/msru` at SHA-256 `bf6a7db48f7347e7ec4541293f3b535d291008cd555b1743b22aced9588dff19`; one H1, five H2, 25 H3, 133 paragraphs, five story sections, three `/open` actions, and zero forbidden dash or arrow characters remain. Isolated Chromium `151.0.7922.34` covered 375, 430, 768, 1024, 1440, and 1920 px; Firefox `153.0` and Playwright WebKit `26.5` covered `390x844` and `1440x900`. Hero, fixed-side chapter composition, mobile linear flow, proof inserts, horizontally contained benchmark, final CTA, keyboard focus, delayed rail, rail non-overlap, header hide and return, CTA navigation to local `/open`, console, page, network, HTTP, and horizontal overflow checks passed. Firefox used viewport screenshots because its full-page screenshot limit is lower than the document height. This pass was not deployed; no public visual verification was run.
- 2026-08-28 local Russian editorial-dossier v2 check: isolated Chromium `151.0.7922.34` passed 375×812, 430×932, 768×1024, 1024×768, 1440×900, and 1920×1080. Firefox `153.0` and Playwright WebKit `26.5` passed viewport journeys at 390×844 and 1440×900; Firefox used viewport screenshots because its full-page PNG limit is below this document height. Hero, chapter strip, fixed-duration hash navigation, delayed rail and non-overlap, proof-note desktop and mobile states, quote spread, benchmark, 4.4× conclusion, anti-filter, inventory, FAQ interaction, final CTA, keyboard focus, JSON-LD, console, page, network, HTTP, and horizontal overflow checks passed. One H1, five story H2 headings, five story sections, four `/open` links, and byte-identical Russian route mirrors remain. The OG image is 571,004 bytes. Production and the English page were not checked or changed because this pass was not deployed. A real iPhone was not tested.
- 2026-08-28 Russian editorial-dossier v2 deploy verified: the clean `90`-file static release is `/var/www/coin.im/releases/20260828T052802Z`. All 23 checked public HTTPS URLs return 200, including `/msru`, `/msru/`, the new OG image, `/open`, `/api/open/health`, and both checked `www` URLs. Public `/msru`, `/msru/`, `assets/msru.css`, `assets/msru.js`, and `assets/coin-im-msru-og.png` match the local SHA-256 values. Public isolated Chromium `151.0.7922.34` passed `390×844` and `1440×900`: chapter navigation, benchmark content, FAQ interaction, final CTA navigation to `/open`, console, page, network, HTTP, keyboard focus, and horizontal overflow checks passed. Nginx configuration is valid and Nginx plus `coin-im-open` are active. The backend remains `/opt/coin-im-open/releases/20260817T170404Z`. A real iPhone was not tested.
- 2026-08-28 English editorial-dossier v2 deploy verified: the clean `91`-file static release is `/var/www/coin.im/releases/20260828T060451Z`. All 22 checked public HTTPS URLs return 200, including `/ms`, `/ms/`, `/msru`, `/msru/`, both Market Scan OG images, `/open`, `/api/open/health`, and `www`. Public `/ms` and `/ms/` match the local English SHA-256 above; `/msru` and `/msru/` remain byte-identical at `a87a5a1c547d5634d9fe604a52415351728ce99917e4b760e93a577fec4d39d2`. Shared CSS hashes to `c352482576164752eb647fbc49bf2a97f2cb0cbb4c5582a61cb265b82f996ae6`, shared JavaScript to `ca47f60c3e97e7bcc6544940856d2c6af2e4c2de72eaa982d94426d75fd0f590`, and the English OG image to `4d03182785bb6f1da438e9d3d5108905c526bfb380d0cafb03e256aced1914de`. Local isolated Chromium `151.0.7922.34`, Firefox `153.0`, and Playwright WebKit `26.5` verified the hero, chapter navigation, benchmark, FAQ, focus state, console, page, network, HTTP, and horizontal overflow; the final post-typography gate covered `390×844`, `768×1024`, and `1440×900` in Chromium and WebKit plus `390×844` and `1440×900` viewport evidence in Firefox. Public Chromium passed `390×844` and `1440×900`, including the final CTA navigation to the live `/open` form. Nginx and `coin-im-open` are active; the backend remains `/opt/coin-im-open/releases/20260817T170404Z`. A real iPhone was not tested.
- 2026-08-28 homepage and `/mail` route deploy verified: the clean `94`-file static release is `/var/www/coin.im/releases/20260828T081540Z`. Root `/` serves the English `Coming soon.` page with only the large `/ms` and `/mail` links; `/mail` and `/mail/` serve the complete former English homepage body unchanged. All 27 checked public HTTPS URLs return 200. Public root, both `/mail` routes, `assets/coming-soon.css`, `llms.txt`, `sitemap.xml`, `/ms`, and `/msru` match local SHA-256 values. Public isolated Chromium `151.0.7922.34` passed `390×844` and `1440×900`, including keyboard focus and real clicks from root to `/ms` and `/mail`, without console, page, network, HTTP, or horizontal-overflow errors. Nginx configuration is valid and Nginx plus `coin-im-open` are active. The backend remains `/opt/coin-im-open/releases/20260817T170404Z`. A real iPhone was not tested.
- Post-restyle check of the Market Scan benchmark matrix: `.data-table` survives the new type scale. On production `/msru` the table renders at 610 px inside the narrower measure, monospace at 13.44 px, five rows with the `data-table-peak` accent on the GPT 5.6 Sol Max row, one H1, no horizontal overflow, `document.fonts.status` loaded. At 375 px the stacked rules still apply: `thead` hidden, cells display grid, `data-label` values rendering through `::before` in Russian, no overflow. The `GLM5.2 max через Qwen CLI` cell and the caption now wrap to two lines, which is the only visible change from the restyle. Fifteen HTTPS checks returned 200, including the new `assets/fonts/source-serif-4-variable.woff2`
- Main production URL: `https://coin.im/`
- Russian page URL: `https://coin.im/ru`
- Spanish page URL: `https://coin.im/es`
- Market Scan page URL: `https://coin.im/ms`
- Russian Market Scan page URL: `https://coin.im/msru`
- File handling page URL: `https://coin.im/handling`
- Business File intake URL: `https://coin.im/open`
- AI/notes page: `https://coin.im/ai.html`, intentionally `noindex, nofollow`
- Short/notes page: `https://coin.im/s.html`, intentionally `noindex, nofollow`

## Local Rollback Snapshot

- Snapshot label: `2026-08-15-130041-bkk-vps-current-rollback`
- Snapshot time: `2026-08-15 13:00:41 BKK`
- Purpose: local clean static rollback point for the current VPS production site.
- Local snapshot directory: `local-releases/20260815T130041BKK-vps-current-clean-static/`
- Local archive: `local-releases/20260815T130041BKK-vps-current-clean-static/site.tar.gz`
- Archive SHA-256: `b4eb773a18e5b1d0a00c6fbac6c060b377ac9e423a39bd7e687eb0b70675299b`
- File count in clean static snapshot: `56`
- Included files follow the VPS production list in `DEPLOY.md`: HTML pages, `llms.txt`, CSS files, `robots.txt`, `sitemap.xml`, `ms/index.html`, and `assets/`.
- To roll back later, deploy this archive through the VPS release flow in `DEPLOY.md`.

## Git Checkpoint

- Annotated tag: `checkpoint-20260815-g31`
- Branch at creation: `main`
- Scope: the complete source state through G31, including the current static site, Market Scan route mirror, `/handling`, the manual encrypted `/open` intake and API source, deployment documentation, audit reports, and G31 homepage copy pass.
- Generated `qa-screens/` files are intentionally excluded; they are local evidence, not required to build or restore the site.
- Inspect the checkpoint with `git show checkpoint-20260815-g31`.
- Start a safe rollback branch with `git switch -c restore/checkpoint-20260815-g31 checkpoint-20260815-g31`. Do not reset a dirty working tree.

## Current Production Deployment

- Deployment date: `2026-08-29`
- Host: `iva` / `193.181.215.57`
- Active release: `/var/www/coin.im/releases/20260829T071346Z`
- Active takeover API release: `/opt/coin-im-market/releases/20260829T054732Z`
- Active intake API release: `/opt/coin-im-open/releases/20260817T170404Z` (unchanged by this static deploy)
- Current symlink: `/var/www/coin.im/current`
- Nginx config: `/etc/nginx/conf.d/coin.im.conf`
- Certbot issued a Let's Encrypt certificate for `coin.im` and `www.coin.im`; it expires on `2026-11-13` and is configured for automatic renewal.
- Public HTTPS checks passed for the root, takeover, archive, stats, rules, market health, every preserved language and product route, the new checkout assets, crawler files, and `www`. The market backend remains isolated at `127.0.0.1:8782`; the existing intake backend is unchanged. Nginx access logging is disabled for private receipt paths. The deployment did not change DNS or mail routing.

## Versioning Rule

When `styles.css` changes, bump the CSS query string in both:

- `index.html`
- `ru.html`

Use date-based keys, for example:

```text
styles.css?v=YYYYMMDD-N
```

Update this file after any completed deploy or major local visual iteration.

## Last Verified Local QA

The 2026-08-27 Russian plain-language pass replaces the hero headline, lead, meta description, Open Graph description, Twitter description, and JSON-LD description with reader-facing language. The first screen no longer mentions internal versions, autonomous runs, benchmark runs, or unexplained breakage. It states that a ready-made list covers only the market the buyer already knew how to search, then explains Market Scan through four concrete questions: which other companies fit, who decides, what to offer, and where to send the physical letter. `msru.html` and `msru/index.html` are byte-identical at SHA-256 `831aedba8ef5055c899970922662312535e402b237dca19d8f5f12d24b3c4c58`. Local and public Chromium `151.0.7922.34` passed `390x844` and `1440x900` without horizontal overflow, console errors, page errors, failed requests, or HTTP errors. The public release is `/var/www/coin.im/releases/20260827T141011Z`; 21 HTTPS checks returned 200; public `/msru` and `/msru/` match the local hash. Nginx and `coin-im-open` are active; backend remains `/opt/coin-im-open/releases/20260817T170404Z`. Evidence is local-only in `qa-screens/msru-plain-language-20260827/` and `qa-screens/msru-plain-language-20260827-public/`. A real iPhone was not tested.

The 2026-08-27 Russian editorial-cover pass replaces the previous side-by-side hero with a different composition and sales angle. The H1 now asks why a bought list ends where the real market begins; the factual lead explains the first ten versus the next thirty directions, the buyer, argument, physical route, four versions, and fifteen autonomous runs. On desktop the headline and lead form an editorial cover above a wide horizontal crop of the real key letter. Mobile keeps a compact key-letter crop above the headline. The Russian header now uses the existing envelope logo, `coin.im`, and `Market Scan`; it is in normal flow and leaves the viewport during reading. `/msru` keeps one H1, five H2, 25 H3, 132 paragraphs, five sections, three in-story `/open` actions, the delayed fixed rail, analytics, and all benchmark facts. `msru.html` and `msru/index.html` are byte-identical at SHA-256 `e958799e1316ef448396de64b5fdebd0273c3dbd243e77abcbf3b6b0fe9c376d`; English `/ms` remains unchanged. Local Chromium `151.0.7922.34` and Playwright WebKit `26.5` passed `390x844`, `1440x900`, and `1920x1080`; Firefox `153.0` passed `390x844` and `1440x900` with targeted viewport screenshots. The unpinned header, first and second screen, keyboard focus, delayed rail, and non-overlap passed without horizontal overflow, console errors, page errors, failed requests, or HTTP errors. The public release is `/var/www/coin.im/releases/20260827T140619Z`. Twenty-one HTTPS checks returned 200; public `/msru` and `/msru/` match the local hash; public Chromium passed the same three viewports and delayed-rail simulation. Nginx and `coin-im-open` are active; backend remains `/opt/coin-im-open/releases/20260817T170404Z`. Evidence is local-only in `qa-screens/msru-cover-redesign-20260827/` and `qa-screens/msru-cover-redesign-20260827-public/`. A real iPhone was not tested.

The 2026-08-27 Russian conversion-story pass removes the early `/open` button, `Читать историю`, part marker, resume prompt, and mobile contents block from `/msru`, so the hero moves directly into part one. The hero now leads with the hidden-market problem and the documented 17-year list history. Five section labels and all 25 H3 headings are outcome-led reading hooks; facts, benchmark values, body copy, analytics, route IDs, and the three established in-story `/open` actions remain. The wide rail is larger, fixed, hidden for the first two minutes and throughout part one, then appears from part two without overlapping the reading measure. Russian HTML now has one H1, five H2, 25 H3, 132 paragraphs, five sections, and three `/open` actions. `msru.html` and `msru/index.html` are byte-identical at SHA-256 `693b139d7ffc5dc931b13cc7f9e2bba9db470126700f65f12c164fd67cd12d13`. English `/ms` remains byte-identical at `42afb6e5496e30a60538e2ad9ccaa21c8d1992e29d6d9e508d7f1bf7079b16f8`. Local Chromium `151.0.7922.34` passed at `390x844`, `1440x900`, and `1920x1080`; Firefox `153.0` and Playwright WebKit `26.5` passed at `390x844` and `1440x900`, with Firefox using viewport screenshots because its full-page screenshot limit is below the document height. No tested engine showed horizontal overflow, console errors, page errors, failed requests, or HTTP errors. The public release is `/var/www/coin.im/releases/20260827T135221Z`. Twenty-one HTTPS checks returned 200; public `/msru` and `/msru/` match the local hash; public Chromium passed the same three viewports, keyboard focus, the early hidden rail, and the simulated two-minute delayed rail without overlap. Nginx and `coin-im-open` are active; backend remains `/opt/coin-im-open/releases/20260817T170404Z`. Evidence is local-only under `qa-screens/msru-conversion-20260827-final/`, `qa-screens/msru-conversion-20260827-cross/`, and `qa-screens/msru-conversion-20260827-public/`. A real iPhone was not tested.

The 2026-08-27 English Market Scan reference pass replaced the previous English longread with a complete translation of the current Russian source and applied the same canonical UI. Both languages have identical protected tag order: one H1, five H2, twenty-five H3, 134 paragraphs, five story sections, and four `/open` actions. `ms.html` plus `ms/index.html` are byte-identical at SHA-256 `42afb6e5496e30a60538e2ad9ccaa21c8d1992e29d6d9e508d7f1bf7079b16f8`; `msru.html` plus `msru/index.html` are byte-identical at `60e99b2f0c260703de787a9eeec892021ca87046bc49b519fa8a12b74b1b09fb`. JSON-LD, IDs, hash targets, assets, route mirrors, forbidden punctuation, Cyrillic leakage, image sizes, CSS balance, and JavaScript syntax passed. Isolated Chromium `151.0.7922.34` covered 375, 430, 768, 1024, 1440, and 1920 px; Firefox `153.0` and Playwright WebKit `26.5` covered `390x844` and `1440x900`. The hero, physical-letter crop, five-part path, mobile and desktop contents, benchmark table, closing block, keyboard skip link, and CTA to `/open` passed without horizontal overflow, console errors, page errors, failed requests, or HTTP errors. Firefox used targeted viewport screenshots because its full-page screenshot limit is lower than the document height. WebKit is an engine-compatibility check, not a real iPhone test. The clean 89-file VPS release is `/var/www/coin.im/releases/20260827T072611Z`. Twenty-one public HTTPS checks returned 200; public and direct-origin route hashes match local files; Nginx validation and reload passed; Nginx and `coin-im-open` remain active; and the backend stays at `/opt/coin-im-open/releases/20260817T170404Z`. Public Chromium checks at `390x844` and `1440x900` passed the reading path, mobile and desktop navigation, benchmark, closing action, and CTA to `/open` with no browser or network errors. Evidence is in `qa-screens/ms-reference-en-20260827/` and remains local-only. DNS, mail, sitemap, backend code, and intake data were not changed.

The 2026-08-27 Russian Market Scan reference UI pass makes `/msru` the canonical visual and interaction baseline for future Market Scan language pages. The approved copy remains intact: all 165 protected H1, H2, H3, and paragraph nodes still match the pre-UI SHA-256 baseline `f20b362d1116f804663f0cd9671dc4fb290b996899dd039369a5ef8356a4cbd9`. The page has one H1, five story sections, four `/open` links, valid JSON-LD, resolved IDs and local assets, zero long dash, en dash, or arrow characters, and byte-identical `msru.html` plus `msru/index.html` at SHA-256 `b2765ddef41b77f0589c3597ae2f05832617f22bee4b814990597aaa27ef7e2a`. Isolated Chromium `151.0.7922.34` checks covered 375, 430, 768, 1024, 1440, and 1920 px; Firefox `153.0` and Playwright WebKit `26.5` covered `390x844` and `1440x900`. The physical-letter crop, five-part reading path, mobile contents, desktop contents rail, benchmark table, closing block, keyboard skip link, and hero navigation to `/open` passed without horizontal overflow, console errors, page errors, failed requests, or HTTP errors. Firefox used viewport screenshots because its full-page screenshot limit is lower than this long document. WebKit is an engine-compatibility check, not a real iPhone test. The clean 89-file VPS release is `/var/www/coin.im/releases/20260827T070439Z`. Twenty-one public HTTPS checks returned 200; public and direct-origin `/msru` plus `/msru/` match the local SHA-256; Nginx validation and reload passed; Nginx and `coin-im-open` remain active; and the backend release remains `/opt/coin-im-open/releases/20260817T170404Z`. Public Chromium checks at `390x844` and `1440x900` passed the first screen, five-part path, mobile and desktop contents, benchmark, closing block, and hero CTA to `/open` without horizontal overflow, console errors, page errors, failed requests, or HTTP errors. Evidence is in `qa-screens/msru-reference-20260827/` and remains local-only. The English `/ms`, DNS, mail, and backend were not changed.

The 2026-08-27 Russian Market Scan sales pass used the explicit `$sales` corpus to sharpen the reader-facing offer without importing claims from the examples. The H1 now names the decision that must be made before another list is bought; the subtitle and metadata state the concrete output; the public manual Business File intake is distinguished from autonomous benchmark runs; the end qualifies who Market Scan is for; and all three primary CTAs now say `Передать материалы для скана`. No CSS, JavaScript, sitemap, backend, DNS, or mail configuration changed. `msru.html` and `msru/index.html` are byte-identical at SHA-256 `24bf0c52993361a2880d450856817c36b0e357f00c419c8bee8c8b688997d982`; JSON-LD parses; IDs and local references resolve; one H1, five H2 headings, three matching `/open` CTAs, and zero long dash, en dash, or arrow characters remain. Local and public isolated Chromium `151.0.7922.34` checks at `390x844` and `1440x900` found no horizontal overflow, console errors, page errors, failed requests, or HTTP errors; the final CTA opens `/open`. The clean 87-file static VPS release is `/var/www/coin.im/releases/20260827T063558Z`. Nineteen public HTTPS checks returned 200, nginx config validation and reload passed, nginx and `coin-im-open` remained active, the backend release remained `/opt/coin-im-open/releases/20260817T170404Z`, and public `/msru` plus `/msru/` hash to the local file. Evidence is in `qa-screens/msru-sales-20260827/` and remains local-only.

The 2026-08-27 Russian Market Scan deduplication pass used the explicit `$anton` voice rules to remove repeated conclusions after their facts were already established. It removed the recap after part one, the repeated recap after part three, the second explanation of paper economics in part five, the final three-point recap, and smaller local duplicates around industry vocabulary, autonomous execution, and model failures. The page went from 6750 to 6018 words. Unique facts, numbers, model names, table values, restrictions, three `/open` CTAs, and the five-part structure remain. `msru.html` and `msru/index.html` are byte-identical at SHA-256 `f4186ba1b37e7a063f0e56a5ee206bfab9ef8d854a7a5769ba7807a1dd184c72`; JSON-LD parses; IDs and hash targets resolve; no forbidden dash or arrow characters were introduced. Local isolated Chromium `151.0.7922.34` checks at `390x844` and `1440x900` found no horizontal overflow, console errors, page errors, failed requests, or HTTP errors; H1, part five, closing copy, keyboard focus, and the final CTA to `/open` passed. The clean static VPS release is `/var/www/coin.im/releases/20260827T055816Z`. Nineteen public HTTPS checks returned 200, nginx config validation and reload passed, nginx and `coin-im-open` remained active, public `/msru` and `/msru/` hash to the local file, and the same two Chromium viewports passed on the public page. Evidence is in `qa-screens/msru-copy-20260827/` and remains local-only.

The 2026-08-15 G44 Russian Market Scan colon cleanup pass applied all 95 requested post-G42 replacements to `msru.html` and mirrored the result to `msru/index.html` without changing CSS or files under `assets/`. `msru.html` and `msru/index.html` are byte-identical. The requested local report block matched for both files: visible body text contains 63 colons after removing `head` and `script`; long dash and arrow counts are zero; `Машина: наоборот`, `Судья: отдельный`, `Титул: улика`, and `С бумагой: нет` are absent; and opening plus closing `div` counts are 19 and 19. Browser checks on local `/msru` at 375, 768, 1024, 1440 and 1920 returned one H1 and `document.body.scrollWidth === window.innerWidth` as true; 25 screenshots, one viewport from each of five parts at each width, are in `qa-screens/g44-msru/`. The VPS release was built from the then-current active snapshot `/var/www/coin.im/releases/20260815T150720Z` and only `msru.html` plus `msru/index.html` were replaced from the local tree, preserving unrelated dirty work and existing assets. The active 83-file VPS release is `/var/www/coin.im/releases/20260815T151012Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Nginx is active, `nginx -t` passes, the `coin.im` certificate remains valid until `2026-11-13`, and local/production hashes match for `msru/index.html`.

The 2026-08-15 G43 Market Scan pass removed the five visible `.ms-part-number` rows from English `/ms`, leaving the following H2 headings unchanged and leaving `.ms-part-number` in `assets/ms.css` unused. `ms.html` and `ms/index.html` are byte-identical. Local checks returned `ms-part-number=0`, `<h2=5`, `<h1=1`, and `msru.html ms-part-number=0`; the prompt expected six `<h2` matches, but the current English page has five part headings and G43 explicitly did not change headings. Production release `/var/www/coin.im/releases/20260815T150720Z` was built from the active VPS snapshot and removed only those five rows in `ms.html` plus `ms/index.html`; non-target files checked against the previous active release were unchanged, including `index.html`, `/msru`, `/open`, and `assets/ms.css`. Public checks passed for `/`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/open`, `/api/open/health`, and `https://www.coin.im/`; public `/ms` returns `ms-part-number=0`, `<h2=5`, and `<h1=1`, and `/ms` plus `/ms/` hashes match. Production browser checks at 375, 768, 1024, 1440 and 1920 found one H1, no `.ms-part-number` elements, no horizontal overflow, and no console warnings or errors. Section top gaps above the five H2 headings stayed at 88 px on 375, 112 px on 768 and 1024, and 128 px on 1440 and 1920; screenshots are in `qa-screens/g43-public/`. Nginx is active, `nginx -t` passes, and the `coin.im` certificate remains valid until `2026-11-13`.

The 2026-08-15 G42 Russian Market Scan pass applied the requested Russian analog of G41 to `msru.html` and mirrored it to `msru/index.html` without changing CSS or files under `assets/`. It replaced the nine specified copy fragments, removed the duplicated closing line while preserving the earlier paragraph beginning `Пять минут разговора с ботом на входе. А на выходе`, and inserted the requested Real Money Gaming sample arm using the existing `ms-benchmark-display` class. A local inline `white-space: normal` override on the three new display lines prevents the long Russian sample from being clipped while leaving CSS files unchanged. `msru.html` and `msru/index.html` are byte-identical. The requested grep block matched expected counts for both files: zero long dashes, arrows and obsolete phrases; one `четырьмястами шестьюдесятью`; one `сорок две минуты против`; one `Пять минут разговора с ботом`; four `Real Money Gaming`; two `ms-benchmark-display`; and 19 opening plus 19 closing `div` tags. Browser checks on local `/msru` at 375, 768, 1024, 1440 and 1920 returned `document.body.scrollWidth === window.innerWidth` as true and exactly one H1; first-screen and sample-arm screenshots are in `qa-screens/g42-msru/`. The G42 files were deployed in VPS release `/var/www/coin.im/releases/20260815T150441Z`; final verification found the current active release `/var/www/coin.im/releases/20260815T150720Z` also contains the same G42 `msru.html` and `msru/index.html` content. Public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Nginx is active, `nginx -t` passes, the `coin.im` certificate remains valid until `2026-11-13`, and local/production hashes match for `msru/index.html`.

The 2026-08-15 G41 Market Scan pass applied the requested `/ms` replacements after G37, changed the Open Graph locale to `en_GB`, removed the two remaining down-arrow symbols, changed `neighboring` to `neighbouring`, updated the specified benchmark/narrative numbers, and inserted one new arm example using the existing `.ms-benchmark-display` block. CSS, `/assets`, `index.html`, `/msru`, `/open`, and other page files were not changed for this pass. `ms.html` and `ms/index.html` are byte-identical. Local checks for `ms.html` returned `0` for `↓`, `0` for long dash, and `0` for `→`; JSON-LD parses; there is one H1 and the expected five H2 part headings; `.ms-benchmark-display` now appears twice, with three lines in each block. The VPS release was intentionally built from the previous active production snapshot `/var/www/coin.im/releases/20260815T144031Z` and only `ms.html` plus `ms/index.html` were replaced from the local tree, to avoid shipping parallel-session work. The active release is `/var/www/coin.im/releases/20260815T145204Z`; public checks for `/`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/open`, `/api/open/health`, and `https://www.coin.im/` returned 200. Local and production hashes match for `ms.html` and `ms/index.html`; public `/ms` returns `0` for `↓`, `0` for long dash, and `0` for `→`; the new hypothesis-weight sentence, arm sample, `en_GB` locale, and `neighbouring industries` each occur once. `index.html`, `msru.html`, `msru/index.html`, `open.html`, `open/index.html`, `assets/ms.css`, `assets/home.css`, `assets/open.css`, and `assets/open.js` are unchanged from the previous active release. Nginx is active, `nginx -t` passes, and the `coin.im` certificate remains valid until `2026-11-13`.

The 2026-08-15 G39 homepage delivery-range pass applied the 8 supplied `index.html` replacements after G38 without changing CSS or the requested headings, then synchronized `copy/home.en.md` with the resulting visible copy. `node scripts/check-copy.js` passes. Local G39 counts in `index.html` are `courier=11`, `$28=1`, `$110=1`, `$22=0`, `$70 per recipient=0`, `Every letter travels one of two ways=1`, `Either way, delivery comes back=1`, `no stamp is its own signal=1`, dollar signs `5`, long dash `0`, arrow `0`, and `we=6`. The remaining `we` strings are in unchanged meta descriptions, headings, an unchanged text link, and the `what-we-do` id, because G39 forbids changing headings, classes and attributes. The `Production and delivery` section went from 11 to 12 `<p>` elements. Local browser checks at 375, 768, 1024, 1440 and 1920 found one H1, no horizontal overflow, valid JSON-LD, and no console warnings or errors. The JSON-LD contains no price and mentions delivery only in the Service and WebPage descriptions. Local Lighthouse returned Performance 95, Accessibility 96, Best Practices 100 and SEO 100. Screenshots are in `qa-screens/g39/`. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T144031Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Production checks confirmed the G39 control text and new `$28` to `$110` range, absence of the old `$22` and `$70 per recipient` strings, one H1, 12 `#stage-4` paragraphs, no horizontal overflow at 375, 768, 1024, 1440 and 1920, valid JSON-LD, and no console warnings or errors. Local and production hashes match for `index.html`, `ms.html`, `ms/index.html`, `msru.html`, `msru/index.html`, `handling.html`, `handling/index.html`, `open.html`, and `open/index.html`. Nginx is active, `nginx -t` passes, and the `coin.im` certificate remains valid until `2026-11-13`.

The 2026-08-15 G38 homepage first-person pass applied the 43 supplied `index.html` replacements without changing CSS or the requested heading markup, then synchronized `copy/home.en.md` with the resulting visible copy. `node scripts/check-copy.js` passes. Local G38 counts in `index.html` are `we=6`, `our=0`, `us=1`, and `I=45`; `and it stays marked` and `the economics stop working for both of us` are absent; the three control strings `I post paper letters.`, `I have printed, packed and posted more than one million pieces`, and `I would rather post four hundred letters that land` each occur once. Local browser checks at 375, 768, 1024, 1440 and 1920 found one H1, no horizontal overflow, valid JSON-LD, and no console warnings or errors. Local Lighthouse returned Performance 95, Accessibility 96, Best Practices 100 and SEO 100. Screenshots are in `qa-screens/g38/`. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T143122Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Production browser checks at 375, 768, 1024, 1440 and 1920 found one H1, no horizontal overflow, visible G38 control text, valid JSON-LD, and no console warnings or errors. Local and production hashes match for `index.html`, `ms.html`, `ms/index.html`, `msru.html`, `msru/index.html`, `handling.html`, `handling/index.html`, `open.html`, and `open/index.html`. Nginx is active, `nginx -t` passes, and the `coin.im` certificate remains valid until `2026-11-13`.

The 2026-08-15 Russian Market Scan cleanup pass applied the current `AGENTS.md` text rule to `/msru`: removed long dash characters, en dash characters and arrow symbols from `msru.html` and `msru/index.html`, removed visible story-part numerals `01` through `05`, and added the future-session rule against visible story-part numerals to `AGENTS.md`. `msru.html` and `msru/index.html` are byte-identical; `ms.html`, `handling`, and `open` route mirrors remain byte-identical. Static checks found no forbidden dash or arrow symbols, no `.ms-part-number` blocks, no visible `01`-style part markers, valid JSON-LD, one H1, five H2 elements, five story sections, 29 H3 elements, and no missing hash anchors. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T142908Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Nginx is active, `nginx -t` passes, the `coin.im` certificate remains valid until `2026-11-13`, and local/production hashes match for `msru/index.html`.

The 2026-08-15 G37 Market Scan punctuation pass removed the 52 long dash characters and 10 arrow symbols from `ms.html` using the supplied replacements, without changing CSS, `index.html`, section headings or the benchmark display. The permanent rule was added to `AGENTS.md`: Russian and English site text must not use long dash characters, en dash characters or arrow symbols; use periods, commas or colons instead. `ms.html` and `ms/index.html` are byte-identical. Counts in `ms.html` are zero for long dash, en dash, arrow and not-equal symbols; the three control strings `Two million, actually`, `Hypothesis, vertical, arm, company`, and `Twenty-seven segments against twenty-nine is a tie` each occur once. Local browser checks at 375, 768, 1024, 1440 and 1920 found one H1, the five expected H2 part headings, no horizontal overflow, valid JSON-LD, and no console errors. The current page uses the existing three-line `.ms-benchmark-display`; `.data-table tbody tr` returns `0`. Lighthouse on local `/ms.html` returned Performance 100, Accessibility 100, Best Practices 100 and SEO 100. Screenshots are in `qa-screens/g37/`. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T142740Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Public `/ms` counts for long dash, en dash, arrow and not-equal symbols are zero, the three control strings each occur once, and local/production hashes match for `ms.html` and `ms/index.html`. Nginx is active, `nginx -t` passes, and the `coin.im` certificate remains valid until `2026-11-13`.

The 2026-08-15 English Market Scan physical-mail correction aligned `/ms` with the `/msru` physical-route structure for Parts 1, 2 and 5 while leaving Parts 3 and 4 unchanged. `ms.html` and `ms/index.html` are byte-identical; `msru.html` and `msru/index.html` are byte-identical; `handling` and `open` route mirrors remain byte-identical. Heading-sequence checks for Parts 1, 2 and 5 match between `/ms` and `/msru` structurally. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T141205Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/ms/`, `/msru`, `/msru/`, `/handling`, `/open`, `/api/open/health`, required CSS and JS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. Nginx is active, `nginx -t` passes, the `coin.im` certificate remains valid until `2026-11-13`, and local/production hashes match for `ms.html`, `ms/index.html`, `msru.html`, and `msru/index.html`.

The 2026-08-15 Russian Market Scan story pass added `/msru` as the Russian version of the `/ms` story page with the supplied hero and Parts 1-5 placed word-for-word, preserving the existing story layout and alternating bands. It added language switchers between `/ms` and `/msru`, hreflang links, sitemap and `llms.txt` entries, and deployment docs for `msru.html` plus `msru/index.html`. Static checks found byte-identical route mirrors, valid JSON-LD, five story sections, one H1, five part numerals, one display block, no lists, pull quotes or images on `/msru`, no missing hash anchors, and no leftover English story-interface strings. Local screenshots at 375 and 1440 rendered the Russian hero without visible overflow. The clean 83-file VPS release is `/var/www/coin.im/releases/20260815T140817Z`; public HTTPS checks passed for `/msru` and required site routes, Nginx is active, the `coin.im` certificate remains valid until `2026-11-13`, and local/production hashes match for `ms.html` and `msru/index.html`.

The 2026-08-15 Business File conversion deploy pass rebuilt `/open` as the Market Chronicle intake page with the requested metadata, backlink above the H1, exact field order, merged Documents uploader, optional “Or message me on” field, and confirmation copy. The current backend still requires `phone` and `channels`, so `assets/open.js` keeps compatibility by sending `phone` as the alternate contact or `email`, `channels` as `["Email"]`, Documents through `company_presentation`, and Anything else as `anything-else.txt`. Route mirrors are byte-identical and `assets/open.js` passes syntax checking. Production checks for `/open` at 390, 768 and 1440 found no horizontal overflow, no old phone field or contact selector, backlink before H1, and the H1 `Get your Market Chronicle`. The clean 81-file VPS release is `/var/www/coin.im/releases/20260815T135602Z`; required HTTPS routes and versioned assets returned 200, Nginx is active, and Certbot reports the `coin.im` certificate valid until `2026-11-13`.

The 2026-08-15 Market Scan story visual pass preserved all 156 approved `h1`, `h2`, `h3` and body-paragraph text nodes byte-for-byte at normalized-copy SHA-256 `6a3bc528f4b68a6ddd4a367b6b7d770a15ebbf226ac4501c5976fa6d532a5cbd`. It replaced the requested title, description, Open Graph and matching Twitter metadata; added the hero CTA/read link and bottom marker; introduced five numbered full-width story bands with alternating `#f4efe5` and `#ece4d6` backgrounds; removed visual rules between parts; and raised the Market Scan CSS key to `20260815-story1`. Local browser checks at 375, 430, 768, 1024, 1440 and 1920 found no horizontal overflow or centered body text, correct band colors and numerals, a natural-height mobile hero, an approximately 85% viewport desktop hero, and no console warnings or errors. `ms.html` and `ms/index.html` are byte-identical; JSON-LD parses and all internal and ARIA targets resolve.

The 2026-08-15 combined G33/G35 pass restored the pre-G31 homepage heading, replaced the hero lead as supplied by G35, returned the inter-stage band to its empty decorative state, moved the lazy key-letter plate after all four stages, removed the explanatory proof block under `1,000,000+`, and applied all 35 supplied copy replacements. Public CSS and plate image markup are unchanged. `copy/home.en.md` matches the resulting page; `scripts/check-copy.js` now follows the new DOM section order. Local and production checks at 375, 768, 1024, 1440 and 1920, plus production at 430, found one `h1`, 12 `h2` elements, valid JSON-LD and ARIA targets, no horizontal overflow, no console warnings or errors, and the key plate below the first fold with `loading="lazy"`. Lighthouse returned Performance 95, Accessibility 96, Best Practices 100 and SEO 100. The supplied negation regex reports two false positives by matching `No` at the start of the two `Notifications` meta-alt values; details are in `audit-g35.md`. The clean 81-file VPS release is `/var/www/coin.im/releases/20260815T131609Z`; required HTTPS routes returned 200, and local/active hashes match for `index.html`, `ms.html`, and `ms/index.html`.

The 2026-08-15 Market Scan copy pass rewrote the `/ms` hero, plain-English premise/order block, difficulty section, naming section and final CTA with the requested practitioner voice and body-only profanity. It also removed specific model labels, banned filler words, remaining hedge words and British spellings from `/ms` copy so the deployed page follows the supplied constraints. No CSS, route targets, stylesheet links, scripts or static hash links changed. `ms.html` and `ms/index.html` are byte-identical; JSON-LD parses; the page has one `h1`; static hash links and `aria-labelledby` targets resolve; local `/ms.html` and `/ms/` returned 200. It was deployed to VPS release `/var/www/coin.im/releases/20260815T124702Z`; required HTTPS routes returned 200; local, active `ms.html` and active `ms/index.html` SHA-256 hashes match at `46aabe8ee23ef7c66f0dab1d84e438769bca6fe650af84bac1810310407e6708`.

The 2026-08-15 G31 pass replaced the homepage hero framing, lead, first-letter caption and previously empty record bridge with the supplied two-piles copy, without changing public CSS, images, component classes, social image alt text or the stage panel. The visible-copy reference was synchronized and `node scripts/check-copy.js` passes. Local and production checks at 375, 768, 1024, 1440 and 1920, plus production at 430, found one `h1`, three lead paragraphs, valid ARIA targets, no horizontal overflow and no console warnings or errors. Lighthouse returned Performance 94, Accessibility 96, Best Practices 100 and SEO 100. The exact COPY renders in three heading lines at 1440 rather than the requested maximum of two; the prompt forbids the CSS, font-size and markup changes needed to alter that, so the discrepancy is documented in `audit-g31.md`. The prompt's case-sensitive lowercase count for `nobody can tell what is inside` is also inconsistent with its uppercase COPY. The clean 81-file VPS release is `/var/www/coin.im/releases/20260815T113449Z`; required HTTPS routes returned 200 and local/active homepage hashes match.

The 2026-08-15 G30 pass replaced all seven supplied homepage stage-panel strings without changing markup or CSS and deployed the accumulated G27-G30 text changes. Stage names remain on one line at 1024, 1280 and 1440; notes stay within two lines; and local overflow checks passed at 375, 768, 1024, 1280 and 1440. The existing 1024 breakpoint stacks the panel below the hero, so the hero ends below the 768 px viewport; this unresolved style constraint and the two inconsistent grep expectations are documented in `audit-g30.md`. Lighthouse returned Performance 94, Accessibility 96, Best Practices 100 and SEO 100. The clean 81-file release is `/var/www/coin.im/releases/20260815T110931Z`; all required HTTPS routes returned 200, production overflow checks passed at 375, 430, 1024 and 1920, and remote hashes match.

The 2026-08-15 G29 pass aligned Market Scan with the homepage by stating that the first chronicle returns within 72 hours in Current status, the visible timing FAQ answer and its JSON-LD counterpart. The visible and structured answers match exactly, JSON-LD is valid, and `ms.html` equals `ms/index.html`. Browser checks at 375 and 1440 found no horizontal overflow or console warnings; Lighthouse returned Performance 99, Accessibility 100, Best Practices 100 and SEO 100. The requested grep expectation says two `72 hours` matches in `ms.html`, but the three required placements necessarily produce three; this is documented in `audit-g29.md`. It was deployed with G30 in `/var/www/coin.im/releases/20260815T110931Z`.

The 2026-08-15 G28 pass applied the four supplied homepage changes: shortened the Stage 3 and `Before a word is read` openings, removed `and pay for most` from Stage 1, and added the supplied six-hundred-and-eighty-business evidence paragraph before `contrast-pair`. The new paragraph is in the required position, the page has one `h1`, and browser checks at 375 and 1440 found no horizontal overflow or console warnings. Lighthouse remained at the current homepage baseline of Performance 94, Accessibility 96, Best Practices 100 and SEO 100. The supplied Stage 3 replacement is not literally a word-preserving sentence split; that discrepancy is recorded in `audit-g28.md`. It was deployed with G30 in `/var/www/coin.im/releases/20260815T110931Z`.

The 2026-08-15 G27 pass removed the eleven specified uses of `quietly` and `genuinely` from Market Scan while preserving the two meaningful `quietly` uses, the one meaningful `genuinely` use and all 19 occurrences of `actually`. A5 was updated in both the visible FAQ and JSON-LD; B5 and B6 occurred only in visible prose. `ms.html` and `ms/index.html` are byte-identical, JSON-LD is valid, and browser checks at 375 and 1440 found no horizontal overflow or console warnings. Lighthouse returned Performance 99, Accessibility 100, Best Practices 100 and SEO 100. Its timing discrepancy was resolved by G29. It was deployed with G30 in `/var/www/coin.im/releases/20260815T110931Z`.

The 2026-08-15 G25 and G26 pass replaced the opening evidence in the homepage `Why paper, when email is free` section and removed the requested qualification cascade, limits section, duplicate FAQ item, status repetition, decorative labels and long dashes from Market Scan. `ms.html` and `ms/index.html` are byte-identical; JSON-LD is valid; visible and structured FAQs contain six questions; and no internal link or `aria-labelledby` target is missing. Browser checks at 375, 768, 1024, 1440 and 1920 found no horizontal overflow and no console warnings or errors. Market Scan Lighthouse returned 100 in all four categories. Homepage Lighthouse remained at the G24 baseline of Performance 94, Accessibility 96, Best Practices 100 and SEO 100. The combined static release is `/var/www/coin.im/releases/20260815T105520Z`; all required public HTTPS checks passed and evidence is in `audit-g25.md`, `audit-g26.md`, `qa-screens/g25/` and `qa-screens/g26/`.

The 2026-08-15 G24 pass consolidated the prior homepage cleanup, shortened the two remaining record statements, restored the empty `home-rest` band, applied the requested label colour and raised both homepage CSS keys to `g24`. The shared `.equation` rule in `base.css` was retained because `/ms` still uses it; the FAQ phrase `delivery scan and nothing else` was retained because G24 supplies no replacement and forbids invented copy. Checks at 375, 768, 1024, 1440 and 1920 found one `h1`, no missing `aria-labelledby` targets, no uppercase homepage labels and no horizontal overflow. Lighthouse returned Performance 94, Accessibility 96, Best Practices 100 and SEO 100; the accessibility finding is the requested low-contrast `var(--ink-soft)` label on the dark plate. It was deployed to VPS release `/var/www/coin.im/releases/20260815T103707Z`; all required public HTTPS checks passed and evidence is in `audit-g24.md` and `qa-screens/g24/`.

The 2026-08-15 G23 pass removed `What we cannot do yet`, `What we will not do`, and `Who should not hire you?` without moving or rewriting surrounding copy. Stage four now moves directly from the delivery-evidence paragraph to `How a record ends`; `Before a word is read` ends with `What is still on the desk on Thursday`; and the FAQ contains six questions, ending with `Which countries can you post to?`. Checks at 375, 768, 1024, 1440 and 1920 found one `h1`, no missing `aria-labelledby` targets and no horizontal overflow. Lighthouse returned Performance 94, Accessibility 100, Best Practices 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T103058Z`; all required public HTTPS checks passed and evidence is in `qa-screens/g23/`.

The 2026-08-15 G21 pass applied all thirteen supplied homepage cuts, removed the remaining equation and its CSS, and preserved `What we cannot do yet`, its three items, `What we will not do`, and `Who should not hire you?`. Checks at 375, 768, 1024, 1440 and 1920 found no horizontal overflow and no equation elements. Lighthouse returned Performance 94, Accessibility 100, Best Practices 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T102258Z`; all required public HTTPS checks passed and evidence is in `qa-screens/g21/`.

The 2026-08-15 brand1 pass replaced the generated SVG coin mark with a targeted physical-direct-mail emblem combining an envelope, key, wax seal and coin rim. The `coin.im` wordmark is approximately 20% larger, and the email, WhatsApp and Telegram links use restrained button treatments with recognizable brand-colour rules. The generated WebP is 128 x 128 px and 4402 bytes. Checks at 375, 430, 768, 1024, 1440 and 1920 found no horizontal overflow, loaded imagery, usable contact targets and an empty console. Lighthouse returned Performance 95, Accessibility 100, Best Practices 100 and SEO 100. It was deployed with the current G19 cleanup to VPS release `/var/www/coin.im/releases/20260815T101647Z`; all required routes and the new logo returned HTTP 200, and local/active HTML, CSS and logo hashes match. Production evidence is in `qa-screens/brand1/production-1440.png`.

The 2026-08-15 G19 pass applied the supplied homepage cleanup literally: removed non-informational labels, long dashes, link arrows, decorative record numbering, the read-on control and the duplicate equation; shortened the 12 object-use openings mechanically; and added the approved record-to-market bridge. Checks at 375, 768, 1024, 1440 and 1920 found one `h1`, one remaining `≠`, the preserved `#object-one` anchor, no horizontal overflow and empty consoles. Lighthouse returned Performance 88, Accessibility 96, Best Practices 100 and SEO 100. Accessibility is 96 because the required `.plate__label { color: var(--ink-soft) }` has insufficient contrast on the dark plate; it was reported rather than changed outside the prompt. The static site was deployed to `/var/www/coin.im/releases/20260815T101422Z`; evidence is in `audit-g19.md` and `qa-screens/g19/`.

The 2026-08-15 G18 pass removed hover and active geometry changes from homepage buttons, confined the yellow fill to fine-pointer hover, preserved a single inherited pointer cursor and prevented touch hover from sticking. It rebuilt `The record` as one left-aligned 62ch column with the dark `1,000,000+` heading, ledger rules, numbered proof points, separate production-error list and preserved remainder copy. Browser checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth`, matching left edges, `position: static`, a fitting number and no console errors. Hover, touch and reduced-motion checks passed; the 3.4-second recording and screenshots are in `qa-screens/g18/`. Lighthouse returned Performance 95, Accessibility 100, Best Practices 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T095910Z`.

The 2026-08-15 G16 pass added the full-width dark `#object-one` plate immediately after the G14R hero, with the first key letter in AVIF, WebP and JPEG at four responsive widths and the supplied caption copy. The source remains unchanged; the 1280 variants meet their weight budgets and the AVIF text crop is readable. Checks at 375, 768, 1024, 1440 and 1920 found no horizontal overflow, preserved 4:5 geometry, aligned image and caption edges, no image effects or motion, and an empty console. Lighthouse returned Performance 95, Accessibility 100, Best Practices 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T095133Z`; all required routes and the 1280 AVIF returned HTTP 200, and local/active HTML and CSS hashes match. Evidence is in `audit-g16.md` and `qa-screens/g16/`.

The 2026-08-15 G14R pass replaced the previous hero with the supplied coin-tile lock-up, responsive two-column hero, four-stage index, two actions and an `IntersectionObserver` minibar. Checks at 375, 768, 1024, 1440 and 1920 found no horizontal overflow, one `h1`, one stage link and an empty console. Lighthouse returned Performance 95, Accessibility 100, Best Practices 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T093817Z`; all required public routes returned HTTP 200, Nginx and `coin-im-open` remained active, and local/active HTML and CSS hashes match. Evidence is in `audit-g14r.md` and `qa-screens/g14r/`.

The 2026-08-15 wax-dedup pass removed the duplicate wax-seal paragraph from Stage four while preserving the fuller paragraph in the envelope section. The adjacent delivery paragraphs remain direct siblings with no empty container or hanging heading, and `grep -c "closes with wax" index.html` returns `1`. The change is present in the active VPS release `/var/www/coin.im/releases/20260815T093817Z`; Nginx validation passed, the public routes returned HTTP 200, and before/after evidence is in `qa-screens/wax-dedup/`.

The 2026-08-15 G8M pass added the manual `/open` Business File intake and its encrypted API without accounts, status pages, result tokens, SMTP, workers, automatic scans, dashboards or countdowns. Files are streamed into AES-256-GCM ciphertext under `/var/lib/coin-im-open/jobs/<job_id>/files`; metadata is encrypted beside them; the key is `/etc/coin-im-open/encryption.key`; and SSH MOTD plus `sudo coin-open notifications` is the owner notification channel. Production tests returned `422` for missing required data and every private URL range, `415` for a forbidden extension, `413` above 200 MB and `429` on the sixth submission in one hour. A real test file was stored as 60 bytes of ciphertext with no plaintext match, the notification appeared, and `coin-open delete` returned the original filename before removing the whole job. `/handling` stayed byte-identical at SHA-256 `5a6c0cf942f2650deadef4d28cfe8e1525e3394cb4b15ce1cb602493e13664e9`. Production checks at 375, 430, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth`, one `h1`, no `www` links, no third-party resource hosts and an empty console. Lighthouse returned Performance 99, Accessibility 100 and SEO 100. Static and API releases are both `20260815T092516Z`; evidence is in `qa-screens/g8m/`.

The 2026-08-15 G14 pass replaced the hero mail action with one `Open a business file` stamp button to `/open`, added the supplied non-link response note, demoted the Market Scan action to a prose link, and rebuilt the four stage links with supplied subtitles and down arrows. Keyboard focus is visible, marker fill reaches both lines, reduced motion removes the transition, accent contrast is 13.80:1 for ink and 4.80:1 for muted text, and all four anchors land below the fixed header. Checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth`; the actions stack only at the mobile breakpoint. Lighthouse returned Performance 95, Accessibility 100 and SEO 100. It is present in the combined active VPS release `/var/www/coin.im/releases/20260815T092516Z`; all required public routes returned HTTP 200 and local/active HTML and CSS hashes match. Evidence is in `qa-screens/g14/`.

The 2026-08-15 G15 pass rebuilt the English object catalogue as 12 ordered sections with one matching IMG-1/IMG-2 letter image after each text block. The supplied heading, introduction, new copy, closing paragraph and all 12 alt strings match; the first image is eager and the remaining 11 are lazy; the old key specimen, duplicated letter text, obsolete caption and removed object sections are absent. Browser checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth`, preserved 4:5 image proportions, a `--measure-wide` maximum, and an empty console. Lighthouse returned Performance 96, Accessibility 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T091850Z`; production browser checks at 375 and 1440 found 12 sections, 12 image elements, no failed loads, no overflow and an empty console. All 12 production image URLs and the required site, handling, open and API health routes returned HTTP 200. Evidence is in `audit-g15.md` and `qa-screens/g15/`.

The 2026-08-15 G13 pass added the exact approved `Before a word is read` section after the object catalogue and before `Why paper, when email is free`. All 25 supplied COPY blocks match character-for-character; the removable handwritten-address paragraph remains in full; the six `h3` headings retain their specified order; and the section contains no lists, images, icons or new `.rest-band` elements. Local checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth`, one page `h1`, and an empty console; production checks passed at those widths plus 430. Prose width is 667.78 px and minimum measured contrast is 9.31:1. Lighthouse returned Performance 98, Accessibility 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T085115Z`; all required public HTTPS checks passed and local/production hashes match. Screenshots and the Lighthouse report are in `qa-screens/g13/`.

The 2026-08-15 G11 pass added the exact approved `Why paper, when email is free` section between the object catalogue and `How we work together`, omitted the unfilled `0.0%` paragraph entirely, and added the two approved machine-checking paragraphs immediately after the unchanged Market Scan postage sentence. The home page still has three `.rest-band` elements; both pages have one `h1`; `ms.html` and `ms/index.html` are byte-identical; and the supplied COPY blocks match character-for-character. Local and production checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth` with an empty console. Minimum measured contrast is 9.31:1 and maximum text width is 667.78 px. Lighthouse returned Performance 99, Accessibility 100 and SEO 100 for both pages. It was deployed to VPS release `/var/www/coin.im/releases/20260815T083411Z`; all required public HTTPS checks passed. Screenshots and Lighthouse reports are in `qa-screens/g11/`.

The 2026-08-15 infrastructure correction replaced the inaccurate Hetzner/Ashburn storage sentence on `/handling` with the verified Webdock/Copenhagen location while preserving the encryption-at-rest requirement for the planned AES-GCM upload service. Matching `handling.html` and `handling/index.html` files were deployed to VPS release `/var/www/coin.im/releases/20260815T082251Z`; `/handling` returns HTTP 200 with `Referrer-Policy: no-referrer`, the public page contains `Webdock` and no longer contains `Hetzner`, and all required public HTTPS checks passed. G8 was not exposed because OpenAI, Anthropic and SMTP credentials are absent and the current SPF policy authorises only the separate `mail.111.bz` MX.

The 2026-08-15 H1 pass added the factual `/handling` page as matching `handling.html` and `handling/index.html` files. Visible text matches every supplied COPY block after HTML whitespace normalisation, with one `h1`, seven supplied `h2` headings, no unresolved slots, no scripts, no external resources and minimum measured contrast of 9.31:1. Local and production checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth` with an empty console. Production Lighthouse returned Performance 100, Accessibility 100 and SEO 100. Nginx serves `/handling` and `/handling/` with `Referrer-Policy: no-referrer`. It was deployed to VPS release `/var/www/coin.im/releases/20260815T080345Z`. The upload form, status page, cryptographic token generation and token revocation do not exist in this static repository and were not claimed as completed by this pass.

The 2026-08-15 G9 pass added the approved three-line reply-card mechanism and wax-seal explanation in exactly the requested positions without changing CSS, components, images, or the three existing `.rest-band` elements. `node scripts/check-copy.js` passes; `[month]` and `[name]` render literally; measured text contrast is 9.31:1; and local plus production browser checks at 375, 768, 1024, 1440 and 1920 found `scrollWidth === innerWidth` with an empty console. Lighthouse returned Performance 99, Accessibility 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T075410Z`; all required public HTTPS checks passed. Screenshots are in `qa-screens/g9/`.

The 2026-08-15 letter-gallery fix connected all 12 generated IMG-1 and IMG-2 WebP files to the visible `The objects` section on the English homepage. Public browser verification at `https://coin.im/#objects` found 12 DOM images, all 12 loaded, no failed requests, two mobile columns at 375 px, and no horizontal overflow. Local checks at 375, 430, 768, 1024, 1440 and 1920 also found no overflow. It was deployed to VPS release `/var/www/coin.im/releases/20260815T074758Z`; the home CSS cache key is `assets/home.css?v=20260815-letter-gallery`.

The 2026-08-15 audit-anchor pass restored the exact Market Scan anchor sentence `The interface will appear when it has real work to show.` while preserving the caveat as `Not before.`, and restored the second G9 home anchor `Then we design the envelope, mailer, protection, label, and postage around the finished piece—not the other way around. It should arrive clean, secure, and ready to open.` `ms.html` and `ms/index.html` are byte-identical, `node scripts/check-copy.js` passes, the home audit markers remain in place, and all raster images are below 700 KB. It was deployed to VPS release `/var/www/coin.im/releases/20260815T072601Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, `/assets/base.css`, `/assets/home.css`, `/styles.css`, `/robots.txt`, `/sitemap.xml`, and `https://www.coin.im/`. Browser geometry checks for `/` and `/ms` at 375, 430, 768, 1024, 1440 and 1920 found no horizontal overflow and no console, page, or request errors.

The 2026-08-15 IMG-2 asset deployment generated letters 7-12 as complete ImageGen scenes without programmatic text overlays. Manual transcription matched every COPY block on the first attempt, including all three response-card lines in letter 10. The final WebP files are `1600x2000`, below 700 KB, and documented in `audit-img2.md`. VPS release `/var/www/coin.im/releases/20260815T061753Z` passed public HTTPS checks for `/`, `/ru`, `/es`, `/ms`, CSS assets, `robots.txt`, `sitemap.xml`, `https://www.coin.im/`, and all six new IMG-2 assets. CSS cache keys did not change during the IMG-2 work.

The 2026-08-15 proof-layout fix prevents `1,000,000+` from overflowing its grid track into `Over a million letters, posted.` The number now owns a max-content column, the proof section keeps its full desktop width, and the mobile layout remains stacked. Checks at 375, 430, 768, 821, 1024, 1180, 1440 and 1920 found zero overlap and no horizontal overflow; the approved text is unchanged. It was deployed to VPS release `/var/www/coin.im/releases/20260815T061514Z`; the production Retina check measured a 51.19 px gap with zero overlap. Screenshots are in `qa-screens/proof-readable/`.

The 2026-08-15 IMG-1 asset deployment regenerated six direct-mail letter images as whole generator outputs without programmatic text overlays, kept all final webp files at `1600x2000` and under 700 KB, and documented manual transcription/comparison in `audit-img1.md`. It was deployed to VPS release `/var/www/coin.im/releases/20260815T060106Z`; public HTTPS checks passed for `/`, `/ru`, `/es`, `/ms`, CSS assets, `robots.txt`, `sitemap.xml`, and `https://www.coin.im/`. CSS cache keys did not change.

The 2026-08-15 G10 pass added marker-fill and stamp interaction physics to both home-page buttons, strengthened prose-link underlines on hover, and added the exact `What we cannot do yet` copy after the unchanged delivery sentence. Both buttons passed keyboard focus checks, reduced motion disables transitions and rotation, five requested widths have no horizontal overflow, and measured button contrast is 13.797:1. Lighthouse returned Performance 99, Accessibility 100 and SEO 100. It was deployed to VPS release `/var/www/coin.im/releases/20260815T055944Z`; full evidence is in `audit-g10.md` and `qa-screens/g10/`.

The 2026-08-15 LS1 pass replaced the baked-text object catalogue images with one approved, selectable key-letter specimen; the other nine unapproved letter images were removed from active markup. Caveat is self-hosted as WOFF2 with Georgia fallback, the object crop contains no baked text, all five requested widths have no horizontal overflow, and the measured text contrast is 17.316:1. Lighthouse returned Performance 99, Accessibility 100 and SEO 100. Full evidence is in `audit-ls1.md` and `qa-screens/ls1/`.

The 2026-08-15 G7 rhythm pass removed stage numbers from the hero index and all four stage sections, replaced the four stage labels, and consolidated vertical rhythm around `--space-section`, `--space-block`, and `--space-tight`. The visible copy comparison changed only the approved labels and removed numbers. Checks at 375, 430, 768, 1024, 1440 and 1920 found no horizontal overflow; all four stage links reached their original anchors. Full measurements and before/after screenshots are in `audit-home-g7.md`.

The 2026-08-17 release `20260817T173203Z` cut eight passages the owner picked from a proposed list, on both Market Scan pages.

Removed: the machine-has-no-wanting paragraph closing part one, which part five already states at length; the numbers inside the five per-model paragraphs in part four, now that the matrix above them carries the same figures, leaving each model its verdict; the address-failure list inside the routing step in part two, back to a single pointer at part one; the physical-monument sentence in part five, which repeats part one word for word; the third pass at the nobody-brings-this-table joke in part two; the loose-change aside on the end of the LinkedIn paragraph; the London microcement deep-research example; and the three landing-page-and-CRM sentences inside the hammer passage in part three.

Russian body words went 6127 to 6032, English 7607 to 7368. Paragraph counts went 151 to 149 and 157 to 155. No numbers in the surviving text changed, no dash characters introduced, div balance unchanged at 19 and 20 pairs. Both route mirrors stay byte-identical and live `/ms`, `/ms/`, `/msru`, `/msru/` hash to the local files. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. Text-only pass: no browser checks were run, the table added in the previous release was not touched.

The 2026-08-17 release `20260817T171243Z` put the real model names and the benchmark matrix back on both Market Scan pages, at the owner's explicit instruction.

The names had been on the English page in its first build (`3b51d40`) as a `.data-table` matrix and were dropped in `e22fae9` when the page was rebuilt as a story; the Russian page never carried them. Both pages now name Qwen3.8 max, GPT 5.5 xhigh, GPT 5.6 Sol Max, Kimi k3 max and GLM5.2 max via Qwen CLI throughout part four, plus Fable 5 and Opus 5 as the two models dropped from the roster, and Sol, Fable, GPT 5.5 and Opus by name in the part-three account of the July degradation. The aggregate matrix returned as a `.data-table` with MODEL, AVG MIN, SEGMENTS, ARMS, ARMS PER SEGMENT and COMPLETED, the Sol Max row carrying `data-table-peak`. `.data-table` styling already lives in `assets/base.css`, so no CSS changed.

Browser checks were run for this one because it adds a layout block, not just text. On production at desktop width both pages report one H1, no horizontal overflow, and a 743 px table aligned to the prose measure. At 375 px the mobile rules apply correctly: `thead` hides, cells become a stacked grid, the `data-label` values render through `::before` in the page's own language, and `document.body.scrollWidth` equals the viewport at 375. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. `ms.html` and `msru.html` stay byte-identical with their route mirrors, and live `/ms`, `/ms/`, `/msru` and `/msru/` hash to the local files.

The 2026-08-17 release `20260817T170627Z` gave the English Market Scan page the same restoration the Russian page got in `20260817T170201Z`.

`ms.html` received the parallel pass: the tightened lead-ins were restored to their fuller form, and the same eleven passages were added from the source Telegram posts, written for English rather than translated. LinkedIn at `$20,196` over seventeen years and `$99` a month for Sales Navigator; contacts as the most expensive thing in outreach plus domain karma; the six hundred and eighty businesses; six real subject lines from benchmark runs, quoted in their original English; the London microcement deep-research example; the three-to-twenty-thousand arms range; the full address-failure list in the routing step; the week the models degraded; the eight-day version-four run; the bar the owner is aiming for; the arms-per-segment ratios and the two failed first attempts in the fifteen-cell matrix.

Body words went from 6746 to 7607, paragraphs from 147 to 157, opening tags in `main` from 229 to 239. No numbers changed, no dash or arrow characters introduced, no bold, no badges. Straight quotes were used for the subject lines to match the existing `"supply chain solutions"` usage. `ms.html` and `ms/index.html` are byte-identical at 55514 bytes, and live `/ms`, `/ms/` and the local file all hash to `0244f670327b179b1ae7426c9648b4e87d1979ce`. The Russian page is unchanged from the previous release and still hashes to `36f86a12791b261ed856695630cb2073f56d3fc2`. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. Text-only pass: no browser, responsive or Lighthouse checks were run.

The 2026-08-17 release `20260817T170201Z` restored and expanded the Russian Market Scan copy at the owner's request.

The 2026-08-17 tightening pass (`8d81be2`) had cut the vivid detail out of `msru.html`; the owner asked for it back with maximum specificity. All seventeen tightened sentences were restored to their fuller form, and eleven new passages were added from the source Telegram posts in `~/antropic/GladkovChanel/razbor-kanala/01-market-scan`: the LinkedIn subscription total (`$20,196` over seventeen years, `$99` a month for Sales Navigator), contacts as the most expensive part of outreach and domain karma, the six hundred and eighty businesses behind the ten-hypotheses claim, six real subject lines from benchmark runs, the London microcement deep-research example, the three-to-twenty-thousand arms range, the full address-failure list in the routing step, the model-degradation week, the eight-day fourth-version run, the two-hundred-dollar scan and the reaction the owner is aiming for, the arms-per-segment ratios (sixty, thirty-three, fourteen, eleven and a half, six) and the two failed first attempts inside the fifteen-cell matrix.

Body words went from 5401 to 6127, paragraphs from 141 to 151, tag structure from 262 to 272 opening tags. No numbers were changed, no dash or arrow characters introduced, no bold added, no badges or chips added, and the author's register was kept. `msru.html` and `msru/index.html` are byte-identical at 85275 bytes and the live `/msru` response has the same length. `ms.html` (English) was deliberately left untouched, so the two language versions now differ in depth. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. Text-only pass: no browser, responsive or Lighthouse checks were run.

The 2026-08-18 backend release `/opt/coin-im-open/releases/20260818T000000Z-admin` (activated as the current symlink) turned the private page into a working panel for form submissions. Local commit `2670981`. No static release was needed.

`server/coin_open/admin.py` lists jobs from `/var/lib/coin-im-open/jobs`, newest first, decrypting each questionnaire in memory for the request only. A submission shows name, work email, phone, role, channels, website, the upload time, and its files. Files download through the panel; filenames come from the metadata and are stripped of quotes and separators before the disposition header. A submission whose notice is still in `/var/lib/coin-im-open/notifications` is marked new and carries a button that clears the notice. Deleting is deliberately absent from the panel and stays with `coin-open delete` on the host.

Three routes sit behind the single secret and one regular expression, so any other shape falls through to 404: `GET /api/open/panel/<token>`, `POST /api/open/panel/<token>/ack/<job_id>`, and `GET /api/open/panel/<token>/file/<job_id>/<NN.aesgcm>`. Verified on the live host: the panel answers 200, a wrong token answers 404, a download for a job that does not exist answers 404, and `/api/open/health` still answers ok. Rendering was exercised on the host with a synthetic submission held in memory, which touched no production data.

This is an MVP for a single owner by explicit decision: the secret link is the only gate, and login credentials are expected later. The page is served with `no-store` and `noindex`.

The 2026-08-17 releases `20260817T165209Z` and `20260817T165338Z`, with backend release `/opt/coin-im-open/releases/20260817T165054Z`, added tracked calls to action and a private visitor panel. Local commit `92d118d`.

Both Market Scan pages now carry three buttons instead of two: after part two, after the benchmark, and a new one in the closing block. The closing pair puts the offer first and keeps Telegram beside it as a text link. Each button has a `data-cta` name: `story-mid`, `story-benchmark`, `story-end`, `story-end-telegram`.

`assets/ms-track.js` sends one view per tab and one event per click through `navigator.sendBeacon`, with a `fetch` keepalive fallback. It is loaded on `ms`, `msru`, `index` and `ru`. The first static release shipped without it on `ms.html`; `20260817T165338Z` corrected that, and both mirrors stay byte-identical.

`server/coin_open/analytics.py` is a separate module with its own SQLite file at `/var/lib/coin-im-open/analytics.sqlite3`. A visitor is stored as an HMAC of address and user agent under a salt that rotates daily, so no address or user agent is retained and identifiers do not carry across days. Bot filtering is двухслойное: the beacon requires JavaScript, and known crawlers including GPTBot and ClaudeBot are dropped by user agent. Verified on the live host: a browser user agent was counted, GPTBot and ClaudeBot were not.

Two routes were added to the intake app behind the existing `/api/open/` proxy, so the Nginx config was not touched: `POST /api/open/beacon` and `GET /api/open/panel/<token>`. The token is stored at `/etc/coin-im-open/admin-token`, root:coinopen 640, and is not in the repository; without the file the panel answers 404. A wrong token answers 404 as well, verified.

All sixteen HTTPS checks returned 200 after each static release, `nginx -t` passed, nginx reloaded, and `coin-im-open` restarted cleanly with uvicorn reporting a normal startup. The panel and the beacon were exercised over HTTPS. No browser, responsive or Lighthouse checks were run, so the new closing button pair has not been looked at in a viewport.

The 2026-08-17 release `20260817T163803Z` improved reading comfort on both Market Scan pages. Local commit `60af5ed`.

Body copy moved from 1.16rem to 1.24rem on desktop and from 1.06rem to 1.13rem below 700px, with a 64ch cap so the line does not lengthen with the type, and paragraph spacing of 1.15em. Paragraphs use `text-wrap: pretty` and `hyphens: auto`, headings use `text-wrap: balance`. The width cap sits on the paragraphs, not on `.ms-prose`, so `.ms-benchmark-display` keeps its own width and its nowrap figures cannot overflow. Georgia is unchanged: no web font, no loading cost, no layout shift.

`assets/ms-reading.js` was added. It stores scroll depth and the current heading in localStorage under `ms-read:<pathname>`, clears the entry past 95 per cent, and reveals a muted line in the opening screen only when a returning reader opens the page at the top with no hash. The markup is present but `hidden` by default, so a first-time reader sees the opening unchanged. localStorage access is feature-detected and wrapped, so private mode degrades silently.

All sixteen HTTPS checks returned 200, including the new `assets/ms-reading.js` and the updated `assets/ms.css`. `nginx -t` passed and nginx reloaded. Verified by response codes and served file contents only; no browser, responsive or Lighthouse checks were run, so the new type scale and the resume line have not been looked at in a viewport.

The 2026-08-17 releases `20260817T162332Z` and `20260817T162626Z` covered the Russian home page, an Nginx route fix and the Market Scan first screens. Local commits `f41b99d`, `993cd69`.

On `ru.html`, twenty-four text nodes changed. The nine forbidden long dash characters were removed, including the two inside `og:image:alt` and `twitter:image:alt`, and replaced with commas and colons. Twenty words were normalised to the yo spelling already used by the hero line and by the Russian Market Scan page. Two `являются` constructions became direct verbs, and the abstract clause before the broken-part example was dropped, matching the English home page. Tag structure identical at 456 tags, all 27 numbers preserved.

`/etc/nginx/conf.d/coin.im.conf` gained an exact `location = /msru` block mirroring `location = /ms`, so `/msru` now answers 200 directly instead of redirecting 301 to `/msru/`. The previous config is backed up on the host as `coin.im.conf.bak-20260817`. The `/handling` and `/handling/` locations with `Referrer-Policy: no-referrer` were preserved and verified, and `/api/open/health` still answers 200.

On both Market Scan pages the hero call-to-action block and its caption were removed so the reader reaches the story first; the offer remains further down the page. In `assets/ms.css` the hero no longer reserves `min-height: 85svh` and `.ms-hero-marker` no longer uses `margin: auto` to sit at the bottom, which removes the empty band between the opening and part one. `ms.html` with `ms/index.html` and `msru.html` with `msru/index.html` remain byte-identical.

All fourteen required HTTPS checks returned 200 after each release, `nginx -t` passed and nginx reloaded. This layout change was verified by response codes and page contents only; no browser, responsive or Lighthouse checks were run. Still outstanding: `es.html`, `open.html` and `handling.html` contain long dash characters that the project rules forbid.

The 2026-08-17 release `20260817T161459Z` carried a copy pass over the Russian Market Scan page. Local commit `8d81be2`. Seventeen text nodes changed, 5503 to 5339 Russian words. Removed the lead-ins and the line telling the reader the page is worth finishing, cut the cross-section repeats (address failures, the gambling name list, the table nobody will bring, and the machine-has-no-wanting passage that already closes part five), and dropped one unproven intensifier. The document title went from 85 to 53 characters. Tag structure identical at 492 tags, all 20 numbers preserved, no dash characters introduced, and the author's crude register was left intact. `msru.html` and `msru/index.html` remain byte-identical and both live responses hash the same after the redirect. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. Note for future checks: `/msru` answers 301 to `/msru/`, whereas `/ms` answers 200 directly, so a plain curl without redirect following reads the 301 stub. No browser, responsive or Lighthouse checks were run for this text-only pass.

The 2026-08-17 release `20260817T161033Z` carried a home page copy pass. Local commit `db2b30c`. Two text nodes changed, 4909 to 4884 words: the record body no longer repeats the caption under the 1,000,000 figure, and the abstract clause before the broken-part example was dropped. Tag structure identical at 761 tags, all 48 numbers preserved, no dash characters introduced. `copy/home.en.md` was regenerated and `node scripts/check-copy.js home` reports a match. All fourteen required HTTPS checks returned 200, `nginx -t` passed and nginx reloaded. Live home output confirms the new wording and no longer contains the removed sentences. No browser, responsive or Lighthouse checks were run for this text-only pass.

The 2026-08-17 release `20260817T160603Z` deployed the checkpointed 15 August working state together with an editorial pass over Market Scan. Local commit `e22fae9`. `ms.html` and `ms/index.html` are byte-identical and the live `/ms` and `/ms/` responses hash the same. The copy pass changed 20 text nodes, 6827 to 6603 words, removing self-praise, lead-ins and cross-section repeats; facts, numbers, CTAs, links and voice were preserved, the tag structure is identical at 520 tags, and no dash characters were introduced. The document title went from 74 to 55 characters. All fourteen required HTTPS checks returned 200, including `/`, `/ru`, `/es`, `/ms`, `/msru`, `/handling`, `/open`, `/api/open/health`, the CSS assets, `robots.txt`, `sitemap.xml` and `www.coin.im`. `nginx -t` passed, nginx reloaded and is active, and the `coin.im` certificate covering `coin.im` and `www.coin.im` is valid until 2026-11-13. CSS cache keys were not changed by the copy pass. No browser or Lighthouse checks were run for this text-only pass.

The 2026-08-15 live-home review pass restored the three stronger legacy lines, converted the object catalogue from a two-column grid into ten alternating editorial spreads, completed the visible British-spelling pass, and replaced currency/medicine wording with non-regulated physical analogues. Responsive geometry passed at 375, 430, 768, 1024, 1440 and 1920 with no horizontal overflow; all ten catalogue images loaded. The remaining raster-letter reissue is documented in `audit-live-home.md` because no approved set of ten real first paragraphs exists in the repository.

The 2026-08-15 MS-2 local text pass removed all visible `may` occurrences from Market Scan, added the hypotheses hedge, reduced the object section to its main-page link, rebuilt the buyer-first FAQ and final CTA, and updated the document title. `ms.html` and `ms/index.html` are byte-identical. Checks at `375` and `1440` found no horizontal overflow. Lighthouse remained Performance 99, Accessibility 100 and SEO 100, with LCP improving from 1.8 s to 1.7 s. See `audit-ms2.md` for exact-match discrepancies and the two source-state limitations.

The 2026-08-15 G6 copy refresh changed exactly six approved home-page text fragments. `node scripts/check-copy.js` passes, screenshots at `375` and `1440` have no visual regression, and no horizontal overflow was found at either width. Lighthouse returned Performance 100, Accessibility 100 and SEO 100, with LCP 1.5 s.

The 2026-08-13 refresh was checked at:

- `375`
- `430`
- `768`
- `1024`
- `1440`
- `1920`

Key checks passed:

- no horizontal overflow
- readable typography on mobile and desktop
- valid JSON-LD
- no missing image files
- no empty links
- all web images under 700 KB
- no forbidden AI-taxonomy badge phrases
- English and Russian visible text preserved during the last visual-only refresh
